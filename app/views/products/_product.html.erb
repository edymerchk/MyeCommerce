<div class="product-name ">
	<%= link_to image_tag(product.picture.url,:size => "150x150"), product%>  
	<em><%=product.name %> </em>
	<strong><%=number_to_currency(product.sale_price)%></strong>
</div>		